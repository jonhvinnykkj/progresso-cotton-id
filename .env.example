# Database - Configure com sua string de conexão do Neon
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require&channel_binding=require

# Server
PORT=5000
NODE_ENV=development

# Security - JWT Authentication
# IMPORTANTE: Gere um secret seguro para produção (mínimo 32 caracteres)
# Exemplo: openssl rand -base64 32
JWT_SECRET=your-very-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# CORS (opcional - deixe vazio para permitir todas as origens em dev)
CORS_ORIGIN=
